{
  "name": "be-nc-news",
  "version": "1.0.0",
  "description": "bc-nc-news",
  "main": "index.js",
  "scripts": {
    "setup-test-dbs": "psql -f ./db/setup-test.sql",
    "setup-dev-dbs": "psql -f ./db/setup-dev.sql",
    "test-utils": "mocha spec/utils.spec.js",
    "test": "npm run setup-test-dbs && mocha spec/app.spec.js",
    "seed-dev": "npm run setup-dev-dbs && knex seed:run",
    "seed-test": "npm run setup-test-dbs && NODE_ENV=test knex seed:run",
    "migrate:make": "knex migrate:make",
    "migrate:latest": "knex migrate:latest",
    "migrate:rollback": "knex migrate:rollback",
    "seed:prod": "NODE_ENV=production DB_URL=$(heroku config:get DATABASE_URL) knex seed:run",
    "migrate-latest:prod": "NODE_ENV=production DB_URL=$(heroku config:get DATABASE_URL) knex migrate:latest",
    "migrate-rollback:prod": "NODE_ENV=production DB_URL=$(heroku config:get DATABASE_URL) knex migrate:rollback",
    "start": "node listen.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/northcoders/be-nc-news.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/northcoders/be-nc-news/issues"
  },
  "homepage": "https://github.com/northcoders/be-nc-news#readme",
  "dependencies": {
    "express": "^4.17.1",
    "pg": "^7.18.2",
    "knex": "^0.20.11"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.1.0",
    "chai-sorted": "^0.2.0",
    "supertest": "^4.0.2"
  }
}
